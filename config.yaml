# config.yaml
data:
  path: "tests/data/Bitcoin_historical_data_coinmarketcap.csv"
  asset: "BTC-USD"
ga:
  population_size: 10 # Not used in steady-state ProFiT, but kept for config compatibility
  generations: 3 # Number of mutation steps
objectives:
  - "annualized_return"
  # Multi-objective example (uncomment to use NSGA-II):
  # - "sharpe_ratio"
#llm:
#  client: "ollama" # Use "openai" for OpenAI API, "ollama" for local LLM
#  model: "qwen2.5-coder:14b"

llm:
  client: "deepseek" # Use "openai" for OpenAI API, "ollama" for local LLM
  model: "deepseek-coder"  # or "deepseek-chat", "deepseek-reasoner"