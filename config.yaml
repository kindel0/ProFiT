# config.yaml
data:
  path: "tests/data/Bitcoin_historical_data_coinmarketcap.csv"
  asset: "BTC-USD"
ga:
  population_size: 10 # Not used in steady-state ProFiT, but kept for config compatibility
  generations: 3 # Number of mutation steps
objectives:
  - "annualized_return"
llm:
  client: "mock" # Use "openai" for real runs
  model: "gpt-4o"